---
phase: 04-context-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
  - js/state.js
  - js/dom.js
  - js/i18n.js
  - js/ui.js
  - js/app.js
autonomous: true
requirements:
  - UEXP-01
  - UEXP-03

must_haves:
  truths:
    - "Two pill-style mode buttons (Quick/Precise) appear above the Translate button"
    - "Quick mode is selected by default; Precise is disabled when Bitbucket is not connected"
    - "Selected mode persists to localStorage as translate_mode and restores on page load"
    - "When Precise mode is active AND provider is DeepL, a yellow warning banner appears in the provider section"
    - "Warning disappears when mode changes to Quick OR provider changes away from DeepL"
  artifacts:
    - path: "js/state.js"
      provides: "translateMode state field"
      contains: "translateMode"
    - path: "js/i18n.js"
      provides: "i18n keys for mode toggle and DeepL warning"
      contains: "modeQuick"
    - path: "index.html"
      provides: "Mode toggle HTML and DeepL warning banner"
      contains: "mode-toggle"
    - path: "css/styles.css"
      provides: "Mode toggle and warning banner styles"
      contains: "mode-btn"
  key_links:
    - from: "js/ui.js"
      to: "js/state.js"
      via: "App.setState({ translateMode: mode })"
      pattern: "translateMode"
    - from: "js/ui.js"
      to: "index.html"
      via: "App.dom.deeplWarning display toggle"
      pattern: "deeplWarning"
---

<objective>
Add the Quick/Precise mode toggle UI and the DeepL incompatibility warning banner.

Purpose: Users need a clear way to choose between fast (no context) and precise (context-enhanced) translation modes, and must be warned when Precise + DeepL is incompatible.

Output: Mode toggle buttons above translate button, DeepL warning in provider section, all i18n strings for phase 4, new state fields for all phase 4 features, all new DOM references, and all CSS for phase 4 components.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-context-ux/04-CONTEXT.md
@.planning/phases/04-context-ux/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From js/state.js (current state fields):
```javascript
var state = {
  uiLang: 'en', provider: 'deepl', model: '', apiKey: '',
  jsonData: null, languages: [], selected: new Set(),
  translating: false, contextPrompt: '', theme: 'light',
  editedCells: new Map(), fullyTranslatedLangs: new Set(),
  aiTranslatedCells: new Set(), originalTranslations: {},
  importedJsonData: null, proxyUrl: '',
  bitbucketWorkspace: '', bitbucketRepo: '', bitbucketBranch: 'main',
  bitbucketToken: '', bitbucketConnected: false,
  contextResults: new Map(), searchProgress: null,
};
```

From js/dom.js (pattern for adding new refs):
```javascript
App.dom = {
  // ... existing refs ...
  actionSection: document.getElementById('action-section'),
  translateBtn:  document.getElementById('translate-btn'),
  progressSection: document.getElementById('progress-section'),
  providerSection: document.getElementById('provider-section'),
  providerCollapsibleBody: document.getElementById('provider-collapsible-body'),
};
```

From js/i18n.js (pattern for adding translations):
```javascript
var UI_TRANSLATIONS = {
  en: { key: 'English text', ... },
  'zh-TW': { key: '繁體中文文字', ... },
  ko: { key: '한국어 텍스트', ... },
};
```

From index.html action-section (lines 158-181):
```html
<div class="card action-section" id="action-section">
  <button class="btn btn-primary" id="translate-btn" ...>Translate</button>
  <div id="search-progress-section" ...>...</div>
  <div class="progress-section" id="progress-section">...</div>
  <div class="error-msg" id="translate-error"></div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML scaffolding, state fields, DOM refs, i18n keys, and CSS for all Phase 4 UI</name>
  <read_first>
    - index.html (full file — need to see exact insertion points for mode toggle, stepper, warning, retry)
    - js/state.js (current state object to extend)
    - js/dom.js (current DOM refs to extend)
    - js/i18n.js (full file — need all 3 language blocks to extend)
    - css/styles.css (full file — need to add styles following existing patterns)
  </read_first>
  <files>index.html, js/state.js, js/dom.js, js/i18n.js, css/styles.css</files>
  <action>
**js/state.js** — Add 4 new fields to the state object (after `searchProgress: null`):
```javascript
translateMode: 'quick',     // 'quick' | 'precise'
expandedPanelKey: null,     // string key name or null
failedKeys: [],             // [{ key: string, lang: string, error: string }]
currentStep: null,          // 'search' | 'generate' | 'translate' | null
```

**index.html** — Insert mode toggle HTML ABOVE the translate button inside `action-section` (before the `<button id="translate-btn">`):
```html
<div class="mode-toggle" id="mode-toggle">
  <button class="mode-btn mode-btn--active" data-mode="quick" id="mode-quick">
    <span class="mode-icon">&#9889;</span>
    <span data-i18n="modeQuick">Quick</span>
  </button>
  <button class="mode-btn" data-mode="precise" id="mode-precise" title="">
    <span class="mode-icon">&#128269;</span>
    <span data-i18n="modePrecise">Precise</span>
  </button>
</div>
```

Insert DeepL warning banner inside `provider-collapsible-body` at the END (after the existing disclaimer/key elements):
```html
<div class="deepl-context-warning" id="deepl-context-warning" style="display:none">
  <span data-i18n="deeplContextWarning"></span>
</div>
```

Insert progress stepper HTML inside `progress-section` (before the `progress-label` div):
```html
<div class="progress-stepper" id="progress-stepper" style="display:none">
  <div class="stepper-step" data-step="search">
    <span class="stepper-indicator">&#9675;</span>
    <span data-i18n="stepSearch">Search Context</span>
  </div>
  <div class="stepper-divider"></div>
  <div class="stepper-step" data-step="generate">
    <span class="stepper-indicator">&#9675;</span>
    <span data-i18n="stepGenerate">Generate Descriptions</span>
  </div>
  <div class="stepper-divider"></div>
  <div class="stepper-step" data-step="translate">
    <span class="stepper-indicator">&#9675;</span>
    <span data-i18n="stepTranslate">Translate</span>
  </div>
</div>
```

Insert retry section inside `action-section` AFTER the `translate-error` div:
```html
<div class="retry-section" id="retry-section" style="display:none">
  <span class="retry-count" id="retry-count"></span>
  <button class="btn btn-retry" id="retry-btn" data-i18n="retryFailed">Retry Failed</button>
</div>
```

**js/dom.js** — Add these new references to App.dom (after `contextMenuSearch`):
```javascript
modeToggle: document.getElementById('mode-toggle'),
modeQuick: document.getElementById('mode-quick'),
modePrecise: document.getElementById('mode-precise'),
deeplWarning: document.getElementById('deepl-context-warning'),
progressStepper: document.getElementById('progress-stepper'),
retrySection: document.getElementById('retry-section'),
retryCount: document.getElementById('retry-count'),
retryBtn: document.getElementById('retry-btn'),
```

**js/i18n.js** — Add these keys to ALL THREE language blocks:

English (en):
```
modeQuick: 'Quick',
modePrecise: 'Precise',
modePreciseDisabled: 'Connect Bitbucket first',
deeplContextWarning: 'DeepL does not support context-aware translation. Please switch to OpenAI or Gemini.',
stepSearch: 'Search Context',
stepGenerate: 'Generate Descriptions',
stepTranslate: 'Translate',
contextPanelEmpty: 'No code context found for this key.',
contextPanelNoDesc: 'No context description available.',
contextPanelMore: '+{0} more file(s)',
retryFailed: 'Retry Failed',
retryCount: '\u2717 {0} key(s) failed',
cellFailed: 'Failed',
```

Traditional Chinese (zh-TW):
```
modeQuick: '快速',
modePrecise: '精準',
modePreciseDisabled: '請先連接 Bitbucket',
deeplContextWarning: 'DeepL 不支援情境感知翻譯。請切換到 OpenAI 或 Gemini。',
stepSearch: '搜尋情境',
stepGenerate: '生成說明',
stepTranslate: '翻譯',
contextPanelEmpty: '此 key 未找到程式碼使用情境。',
contextPanelNoDesc: '無可用的情境說明。',
contextPanelMore: '+{0} 個檔案',
retryFailed: '重試失敗項',
retryCount: '\u2717 {0} 個 key 失敗',
cellFailed: '失敗',
```

Korean (ko):
```
modeQuick: '빠른',
modePrecise: '정밀',
modePreciseDisabled: '먼저 Bitbucket을 연결하세요',
deeplContextWarning: 'DeepL은 컨텍스트 인식 번역을 지원하지 않습니다. OpenAI 또는 Gemini로 전환해 주세요.',
stepSearch: '컨텍스트 검색',
stepGenerate: '설명 생성',
stepTranslate: '번역',
contextPanelEmpty: '이 키에 대한 코드 컨텍스트를 찾을 수 없습니다.',
contextPanelNoDesc: '사용 가능한 컨텍스트 설명이 없습니다.',
contextPanelMore: '+{0}개 파일',
retryFailed: '실패 항목 재시도',
retryCount: '\u2717 {0}개 키 실패',
cellFailed: '실패',
```

**css/styles.css** — Add ALL Phase 4 styles at the end of the file (before any closing comments):

1. Mode toggle styles (pill buttons with accent active state) — use exact CSS from RESEARCH.md code example
2. DeepL warning banner (yellow/amber with dark mode variant) — use exact CSS from RESEARCH.md
3. Progress stepper (flexbox row with indicators, dividers) — horizontal flex with `gap: 0.5rem`, `.stepper-step` flex items, `.stepper-divider` 2px line, `.stepper-step--active` bold text + filled circle `\25CF`, `.stepper-step--done` checkmark `\2713`
4. Context panel accordion row: `.context-panel-row td` with `padding: 0`, `.context-panel-cell` with padding `0.75rem 1rem`, `.context-panel` with `border-left: 3px solid var(--accent)`, `background: var(--bg-secondary, #f8f9fa)`, dark mode variant
5. Context panel code snippet: `.context-panel-snippet` with monospace font, `.context-panel-code` with `background: var(--bg-tertiary, #edf2f7)`, `font-family: 'Courier New', monospace`, `font-size: 0.8125rem`, `padding: 0.5rem`, `border-radius: 0.25rem`, `overflow-x: auto`, `white-space: pre`
6. Context panel description: `.context-panel-description` with normal font, margin-top 0.5rem
7. Context panel translation fields: `.context-panel-lang-row` flex row with label + input + save button, `.context-panel-input` styled like existing inputs, `.context-panel-save` styled like existing small buttons
8. Context panel "+N more" button: `.context-panel-more` as inline text button (no border, accent color, cursor pointer)
9. Error cell styling: `.editor-table td.cell-error` with `background: #fff0f0`, `color: #c53030`, dark mode variant `background: #2d1b1b`, `color: #feb2b2`, `::before` pseudo-element with content `'\2717 '`
10. Retry section: `.retry-section` flex row with gap, `.retry-count` in error color, `.btn-retry` styled like `.btn` but with warning/red background `#e53e3e`, white text
11. Key column hover: `.editor-table td:first-child` with `cursor: pointer` in tbody rows, subtle hover background
  </action>
  <verify>
    <automated>grep -c "translateMode" C:/Project/frontitude-auto-translator/js/state.js | grep -q "1" && grep -c "mode-toggle" C:/Project/frontitude-auto-translator/index.html | grep -q "[1-9]" && grep -c "modeQuick" C:/Project/frontitude-auto-translator/js/i18n.js | grep -q "[3-9]" && grep -c "deeplWarning" C:/Project/frontitude-auto-translator/js/dom.js | grep -q "1" && grep -c "mode-btn" C:/Project/frontitude-auto-translator/css/styles.css | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - js/state.js contains `translateMode: 'quick'` in the state object
    - js/state.js contains `expandedPanelKey: null` in the state object
    - js/state.js contains `failedKeys: []` in the state object
    - js/state.js contains `currentStep: null` in the state object
    - index.html contains `id="mode-toggle"` div with two buttons `id="mode-quick"` and `id="mode-precise"`
    - index.html contains `id="deepl-context-warning"` div with `style="display:none"`
    - index.html contains `id="progress-stepper"` div with three `stepper-step` children
    - index.html contains `id="retry-section"` div with `id="retry-btn"` button
    - js/dom.js contains `modeToggle:`, `modeQuick:`, `modePrecise:`, `deeplWarning:`, `progressStepper:`, `retrySection:`, `retryCount:`, `retryBtn:`
    - js/i18n.js en block contains `modeQuick: 'Quick'` and `deeplContextWarning:`
    - js/i18n.js zh-TW block contains `modeQuick:` and `deeplContextWarning:`
    - js/i18n.js ko block contains `modeQuick:` and `deeplContextWarning:`
    - css/styles.css contains `.mode-toggle`, `.mode-btn`, `.mode-btn--active`, `.deepl-context-warning`, `.progress-stepper`, `.stepper-step`, `.context-panel`, `.context-panel-code`, `.cell-error`, `.retry-section`, `.btn-retry`
  </acceptance_criteria>
  <done>All Phase 4 HTML elements, state fields, DOM refs, i18n strings (3 languages), and CSS styles exist in the codebase</done>
</task>

<task type="auto">
  <name>Task 2: Mode toggle logic, DeepL warning reactivity, and init wiring</name>
  <read_first>
    - js/ui.js (need to add mode toggle functions and DeepL warning toggle)
    - js/app.js (need to wire init and event listeners)
    - js/state.js (verify new state fields from Task 1)
    - js/dom.js (verify new DOM refs from Task 1)
  </read_first>
  <files>js/ui.js, js/app.js</files>
  <action>
**js/ui.js** — Add these functions (inside the IIFE, before the closing `})()`):

1. `initModeToggle()` function:
   - Read `localStorage.getItem('translate_mode')` — if `'precise'`, set mode to precise; otherwise default to `'quick'`
   - Call `App.setState({ translateMode: mode })`
   - Update button classes: the active mode button gets `mode-btn--active`, the other loses it
   - If `!App.getState().bitbucketConnected`, set `App.dom.modePrecise.disabled = true` and `App.dom.modePrecise.title = App.t('modePreciseDisabled')`
   - Call `updateDeeplWarning()` after setting mode

2. `setTranslateMode(mode)` function:
   - Guard: if mode is `'precise'` and `!App.getState().bitbucketConnected`, return early
   - `App.setState({ translateMode: mode })`
   - `localStorage.setItem('translate_mode', mode)`
   - Update button classes: `App.dom.modeQuick.classList.toggle('mode-btn--active', mode === 'quick')` and same for modePrecise
   - Call `updateDeeplWarning()`

3. `updateDeeplWarning()` function:
   - `var s = App.getState()`
   - `var show = s.translateMode === 'precise' && s.provider === 'deepl'`
   - `App.dom.deeplWarning.style.display = show ? 'block' : 'none'`

4. `updatePreciseButtonState()` function:
   - `var connected = App.getState().bitbucketConnected`
   - `App.dom.modePrecise.disabled = !connected`
   - `App.dom.modePrecise.title = connected ? '' : App.t('modePreciseDisabled')`
   - If not connected and current mode is precise, call `setTranslateMode('quick')`

Export all four: `App.initModeToggle = initModeToggle`, `App.setTranslateMode = setTranslateMode`, `App.updateDeeplWarning = updateDeeplWarning`, `App.updatePreciseButtonState = updatePreciseButtonState`

**js/app.js** — Wire in the init function:

1. In the `init()` function, after `App.initBitbucket()` call, add: `App.initModeToggle()`

2. Add mode toggle click event listeners (inside init or after):
   - `App.dom.modeQuick.addEventListener('click', function () { App.setTranslateMode('quick'); })`
   - `App.dom.modePrecise.addEventListener('click', function () { App.setTranslateMode('precise'); })`

3. In the existing provider select change handler (the `providerSelect` change listener), add `App.updateDeeplWarning()` call at the end so the warning updates when provider changes.

4. After `App.testBitbucketConnection` success callback (in bitbucket.js or app.js where connection status updates), call `App.updatePreciseButtonState()`. If this is in bitbucket.js, the wiring happens there — read bitbucket.js to find where `bitbucketConnected` is set to true and add the call.
  </action>
  <verify>
    <automated>grep -c "initModeToggle" C:/Project/frontitude-auto-translator/js/ui.js | grep -q "1" && grep -c "setTranslateMode" C:/Project/frontitude-auto-translator/js/ui.js | grep -q "[1-9]" && grep -c "updateDeeplWarning" C:/Project/frontitude-auto-translator/js/ui.js | grep -q "[1-9]" && grep -c "initModeToggle" C:/Project/frontitude-auto-translator/js/app.js | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - js/ui.js contains `function initModeToggle()` that reads from localStorage and sets state
    - js/ui.js contains `function setTranslateMode(mode)` that updates state, localStorage, button classes, and calls updateDeeplWarning
    - js/ui.js contains `function updateDeeplWarning()` that checks `translateMode === 'precise' && provider === 'deepl'`
    - js/ui.js contains `App.initModeToggle = initModeToggle`
    - js/ui.js contains `App.setTranslateMode = setTranslateMode`
    - js/ui.js contains `App.updateDeeplWarning = updateDeeplWarning`
    - js/ui.js contains `App.updatePreciseButtonState = updatePreciseButtonState`
    - js/app.js contains `App.initModeToggle()` call
    - js/app.js contains event listener wiring for `modeQuick` and `modePrecise` click events
    - Provider change handler calls `App.updateDeeplWarning()`
  </acceptance_criteria>
  <done>Mode toggle switches between Quick/Precise, persists to localStorage, disables Precise when Bitbucket is not connected, and DeepL warning appears/disappears reactively based on mode + provider combination</done>
</task>

</tasks>

<verification>
- Open the app in browser at localhost:8080
- Mode toggle buttons visible above Translate button
- Clicking Quick/Precise toggles active state
- Refreshing page restores last selected mode from localStorage
- With Bitbucket not connected, Precise button is disabled with tooltip
- Selecting Precise + DeepL shows yellow warning in provider section
- Switching to OpenAI hides the warning
- Switching to Quick mode hides the warning even with DeepL selected
</verification>

<success_criteria>
- Mode toggle UI renders with Quick selected by default
- Mode persists to localStorage key `translate_mode`
- Precise button disabled when bitbucketConnected is false
- DeepL warning visible only when translateMode=precise AND provider=deepl
- All Phase 4 HTML, CSS, state, DOM refs, and i18n strings are in place for Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/04-context-ux/04-01-SUMMARY.md`
</output>
