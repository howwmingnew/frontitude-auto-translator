---
phase: 03-ai-context-generation
plan: 03
type: execute
wave: 1
depends_on: ["03-01", "03-02"]
files_modified: ["js/context.js"]
autonomous: true
gap_closure: true
requirements: ["ACTX-01"]

must_haves:
  truths:
    - "When uiLang is 'zh-TW', context descriptions are generated in Traditional Chinese"
    - "When uiLang is 'ko', context descriptions are generated in Korean"
    - "When uiLang is 'en', context descriptions are generated in English (no change from current)"
  artifacts:
    - path: "js/context.js"
      provides: "Language-aware system prompt for context generation"
      contains: "uiLang"
  key_links:
    - from: "js/context.js"
      to: "js/state.js"
      via: "App.getState().uiLang read in callContextGeneration"
      pattern: "getState.*uiLang"
---

<objective>
Close ACTX-01 verification gap: make AI-generated context descriptions follow the app interface language (EN/zh-TW/ko).

Purpose: REQUIREMENTS.md ACTX-01 requires descriptions in the app UI language. Currently, SYSTEM_PROMPT is hardcoded English and uiLang is never read in context.js.
Output: Modified js/context.js that appends a language instruction to the system prompt when uiLang is not 'en'.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-context-generation/03-VERIFICATION.md

<interfaces>
<!-- From js/state.js: -->
App.getState() returns state object with property uiLang: 'en' | 'zh-TW' | 'ko'

<!-- From js/context.js (current): -->
var SYSTEM_PROMPT = 'You are a UI context analyzer...'; // hardcoded English, line 12
async function callContextGeneration(batchKeys, cache) // dispatcher at line 98, passes SYSTEM_PROMPT to callContextOpenAI/callContextGemini
async function callContextOpenAI(systemPrompt, userContent) // line 40, receives systemPrompt as parameter
async function callContextGemini(systemPrompt, userContent) // line 73, receives systemPrompt as parameter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language-aware system prompt to context generation</name>
  <files>js/context.js</files>
  <read_first>js/context.js, js/state.js, js/i18n.js</read_first>
  <action>
In js/context.js, modify the `callContextGeneration` function (around line 98) to build a language-aware system prompt before passing it to the provider call.

1. Add a language instruction map as a module-level constant after the existing constants (after line 9, before SYSTEM_PROMPT):

```javascript
var LANG_INSTRUCTIONS = {
  'zh-TW': ' Respond entirely in Traditional Chinese (繁體中文).',
  'ko': ' Respond entirely in Korean (한국어).'
};
```

2. In `callContextGeneration` (line 98), after `var s = App.getState();` (line 100), build the effective prompt:

```javascript
var langSuffix = LANG_INSTRUCTIONS[s.uiLang] || '';
var effectivePrompt = SYSTEM_PROMPT + langSuffix;
```

3. Replace the two SYSTEM_PROMPT references on lines 104 and 106 with `effectivePrompt`:
   - Line 104: `rawContent = await callContextOpenAI(effectivePrompt, userContent);`
   - Line 106: `rawContent = await callContextGemini(effectivePrompt, userContent);`

This approach:
- Keeps SYSTEM_PROMPT as a constant (no mutation)
- Only appends suffix for zh-TW and ko; English gets no suffix (empty string fallback)
- The suffix is a clear language instruction that LLMs reliably follow
- No changes needed to callContextOpenAI or callContextGemini since they already accept systemPrompt as a parameter
  </action>
  <verify>
    <automated>cd C:/Project/frontitude-auto-translator && grep -n "uiLang" js/context.js && grep -n "LANG_INSTRUCTIONS" js/context.js && grep -n "effectivePrompt" js/context.js && grep -c "SYSTEM_PROMPT" js/context.js</automated>
  </verify>
  <acceptance_criteria>
    - js/context.js contains a LANG_INSTRUCTIONS object with keys 'zh-TW' and 'ko'
    - js/context.js reads s.uiLang (pattern: getState.*uiLang or s.uiLang)
    - callContextGeneration passes effectivePrompt (not SYSTEM_PROMPT) to callContextOpenAI and callContextGemini
    - SYSTEM_PROMPT constant itself is NOT modified (still a var declaration, not reassigned)
    - LANG_INSTRUCTIONS['zh-TW'] contains 'Traditional Chinese' or equivalent
    - LANG_INSTRUCTIONS['ko'] contains 'Korean' or equivalent
    - When uiLang is 'en' (or any unlisted value), langSuffix is empty string ''
    - grep for "effectivePrompt" returns at least 3 matches (declaration + 2 provider calls)
    - grep for "SYSTEM_PROMPT" returns exactly 2 matches (the var declaration and the effectivePrompt concatenation)
  </acceptance_criteria>
  <done>
    js/context.js reads uiLang from state and appends language instruction to system prompt for zh-TW and ko. English uses no suffix. SYSTEM_PROMPT constant is unchanged. The fix satisfies ACTX-01 sub-clause: "AI description language follows app UI language."
  </done>
</task>

</tasks>

<verification>
1. `grep -n "LANG_INSTRUCTIONS" js/context.js` shows the map with zh-TW and ko entries
2. `grep -n "effectivePrompt" js/context.js` shows it used in callContextOpenAI and callContextGemini calls
3. `grep -n "s.uiLang" js/context.js` confirms state is read
4. The SYSTEM_PROMPT var declaration line is unchanged from its original content
5. No other files are modified (this is a single-file fix)
</verification>

<success_criteria>
- js/context.js generates language-appropriate prompts based on App.getState().uiLang
- English (default) behavior is unchanged -- no suffix appended
- zh-TW appends Traditional Chinese instruction
- ko appends Korean instruction
- ACTX-01 verification gap is closed
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-context-generation/03-03-SUMMARY.md`
</output>
